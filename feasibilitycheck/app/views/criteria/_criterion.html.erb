<tr>
  <td><%= criterion.criterion_type %></td>
  <td><%= criterion.datum.description %></td>
  <td><%= criterion.operator %></td>
  <td><%= criterion.value %></td>
  <td>
    <% if criterion.request.submit != 1 %><%= link_to 'Destroy Criterion', [criterion.request, criterion], :confirm => 'Are you sure?', :method => :delete %><% end %>
  </td>
</tr>